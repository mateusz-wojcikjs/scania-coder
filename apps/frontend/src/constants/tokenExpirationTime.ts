const HOURS: number = 24;
const MINUTES: number = 60;
const SECONDS: number = 60;
const MILLISECONDS: number = 1000;

export const TOKEN_EXPIRATION_TIME: number = HOURS * MINUTES * SECONDS * MILLISECONDS;