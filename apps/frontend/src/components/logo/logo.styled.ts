import styled from "styled-components";
import { StyledComponents } from "../../types/styled-components";

export const LogoStyled: StyledComponents = styled.img`
    width: 100%;
    max-width: 220px;
`;
