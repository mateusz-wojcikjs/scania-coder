import { LoginForm, LoginTemplate } from "../../components";

export const Login: () => JSX.Element = (): JSX.Element => {

  return (
    <LoginTemplate>
      <LoginForm />
    </LoginTemplate>
  );
};
