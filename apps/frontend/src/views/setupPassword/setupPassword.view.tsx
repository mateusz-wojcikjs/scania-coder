import { FC } from "react";
import { LoginTemplate, SetupPasswordForm } from "../../components";

export const SetupPassword: FC = () => {

  return (
    <LoginTemplate>
      <SetupPasswordForm />
    </LoginTemplate>
  );
};
